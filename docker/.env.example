# JWT Secret (minimum 256 bits)
JWT_SECRET=ground-and-grow-super-secret-jwt-key-for-development-only-change-in-production-256-bits

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Email Configuration (set to false for local development)
EMAIL_ENABLED=false
SENDGRID_ENABLED=false

# SMS Configuration (set to false for local development)
SMS_ENABLED=false
TWILIO_ENABLED=false

# Stripe (use test keys)
STRIPE_API_KEY=sk_test_your_test_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
